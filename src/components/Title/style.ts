import styled from "styled-components";

export const Title = styled.div`
    margin-bottom: 10px;

    h1{
        font-size: 3.2em;
        span{
            color: var(--green);
        }
    }

    p{
        margin-top: 10px;
        font-size: 1.7em;
    }
`